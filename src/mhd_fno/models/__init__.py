"""Neural operator architectures.

Planned modules:
    fno.py         Fourier Neural Operator mapping field(t) -> field(t+dt),
                   optionally conditioned on parameters (M_A, Re)
    baselines.py   simpler references (e.g. autoregressive U-Net) for comparison
"""
