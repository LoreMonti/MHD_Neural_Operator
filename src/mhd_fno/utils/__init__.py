"""Shared utilities.

Planned modules:
    config.py      load/validate YAML experiment configs
    logging.py     run logging and (optional) experiment tracking
    seed.py        reproducible seeding across numpy / torch
"""
